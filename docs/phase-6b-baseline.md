# Phase 6B baseline

Recorded 2026-09-14 before modification.

- Branch: `main`, tracking `origin/main`.
- Existing tracked modifications: `docs/session-planner.md`, the Xcode project, app/navigation/live-map/session/API/session-domain files.
- Existing untracked work: `.gitignore`, Phase 6 account/Today docs, Today source trees, Phase 6 fixtures, and `PhaseSixTodayTests.swift`.
- `.gitignore` was preserved. Signing remains Automatic with team `CA4ZH8R7Y7`, developer identity, bundle `com.example.GW2Companion`, and device families 1/2 as recorded in `project.yml`.
- Free space at baseline: 6.6 GiB on the data volume. All Xcode verification uses an explicit `/tmp/gw2companion-phase6b-derived` path; large xcresult bundles are avoided.
- No reset, clean, stash, checkout, discard, or commit was performed.
